# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_library(nimble-steps-serialize STATIC 
  in_serialize.c
  out_serialize.c
        types.c
  pending_in_serialize.c
  pending_out_serialize.c)

include(Tornado.cmake)
set_tornado(nimble-steps-serialize)

target_include_directories(nimble-steps-serialize PUBLIC ../include)


target_link_libraries(nimble-steps-serialize PUBLIC 
  nimble-steps)

